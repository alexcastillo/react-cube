@import '../../constants/constants.styl'

body {
  perspective  10 * $cubeSize
  perspective-origin center -3 * $cubeSize;
}

.container:before {
  content: '';
  display: inline-block;
  height: $cubeSize;
}